trigger:
  - main

pool:
  name: LocalPool

variables:
  projectDir: '.'

steps:
  # Paso 1: Chequear el código
  - checkout: self

  # Paso 2: Verificar versión de Java instalada en el agente local
  - script: java -version
    displayName: "Verificar versión de Java (usando el del agente local)"
    workingDirectory: $(projectDir)

  # Paso 3: Ejecutar compilación y pruebas unitarias
  - task: Gradle@2
    inputs:
      workingDirectory: '$(projectDir)'
      gradleWrapperFile: 'gradlew'
      gradleOptions: '-Dfile.encoding=UTF-8 -Xmx3072m'
      publishJUnitResults: true
      testResultsFiles: '**/build/test-results/test/TEST-*.xml'
      tasks: 'clean build test'

  # Paso 4: Cucumber
  # Se habilitará cuando estén creadas las pruebas de aceptación.

  # Paso 5: Pitest
  # Se habilitará cuando corresponda ejecutar las pruebas de mutación.

  # Paso 6: SonarQube
  # Se configurará siguiendo la guía de SonarQube.